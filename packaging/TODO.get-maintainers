- implement context-map from kerncvs
  * translate emails to bugzilla emails
  * scan patches for non-suse email that belong to suse people
- add KSOURCE_GIT & --kernel_source (-K) support
  * scan for patches that touch the same file or dir
  * do it on supported branches (branches.conf)
  * ^ craft a function to extract supported branches
  * ^ already in git-fixes somewhere
  * fetch branches.conf with curl
- handle "fixes:" and "This reverts commit" up to all commmits?
